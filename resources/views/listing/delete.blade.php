{{-- <!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>delete_listing_details</title>
    </head>
    <body>
      <div>この商品の出品を本当に取り消していいですか？</div>
      <form method="POST" action="{{ route('list.deleted', ['id'=>$item_detail->id]) }}">
        @csrf
        <p>商品名:{{$item_detail->name}}</p>
        <p>金額:{{$item_detail->price}}</p>
        <p>在庫数:残り{{$item_detail->stock_quantity}}個</p> --}}
        {{-- <p>購入者数:{{}}人</p> --}}
        {{-- <input type="submit" value="削除"><br>
    </form>
    </body>
</html> --}}
